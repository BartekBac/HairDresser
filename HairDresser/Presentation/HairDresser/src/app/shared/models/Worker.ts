import { Schedule } from './Schedule';

export class Worker {
  id: string;
  firstName: string;
  lastName: string;
  rating: number;
  schedule: Schedule;
  userPhoneNumber: string;
  userEmail: string;
  userName: string;
  imageSource: string;
}
