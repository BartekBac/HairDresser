import { Address } from './Address';
import { Schedule } from './Schedule';
import { User } from './User';

export class Worker {
  firstName: string;
  lastName: string;
  rating: number;
  schedule: Schedule;
  userPhoneNumber: string;
  userEmail: string;
  imageSource: string;
}
